git config --global --replace-all user.name 4JN24CS074
git config --global --replace-all user.email likithagadade2006@gmail.com

git init

git add file1
git commit -m "file saved"

git add file1
git commit -m "file saved"

git show 32bf

git log -n 1 --pretty=format:"%h - %an, %ar : %s" 32bf

